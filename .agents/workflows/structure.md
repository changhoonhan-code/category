---
description: Script Writing Step 2 — Structure Engineer. Block and pacing structuring.
---

# /structure

> **Agent Skill**: The Structure Engineer (`reviewlens_structure_engineer`) owns this step.

// turbo
1. Run `python tools/filter_category.py --profile structure_engineer` to generate the filtered `data/category_structure_engineer.json`.
2. Read and absorb the rules in `reviewlens_structure_engineer/SKILL.md`.
3. Reference `data/comparison_blueprint.json` (strategic decisions) and `data/category_structure_engineer.json` (filtered read-only reference).
4. Execute structural decisions (Scene Sequence Assembly, Block Allocation, Pacing Profile Assignment, Teaser/Payoff) and save the results as `data/comparison_outline.json`.
5. Upon completion, execute `/draft` for draft creation.
